export const metadata = {
  title: 'Image Accordion',
  description:
    'Image accordions that expand to reveal images and accompanying content, providing a dynamic way to view and interact with visual elements',
};

## hover Accordion

<ComponentCodePreview name='image-hover-accordion' hasReTrigger >

Install the following dependencies

```bash
npm install framer-motion
```

</ComponentCodePreview>

## tailwind-hover-accordion

<ComponentCodePreview name='tailwind-image-accordion' hasReTrigger />
